// Deprecated: admin session data is now stored entirely under the "hostel_auth"
// key by AuthContext.login(). This file is intentionally left empty to avoid
// re-introducing a second, out-of-sync copy of the session in localStorage.
export {};
