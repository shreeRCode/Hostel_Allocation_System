# Frontend

React + Vite + Tailwind CSS client for the Smart Hostel Management System.

See the [project README](../README.md) for setup instructions, environment
variables, and an overview of the whole system.

```bash
npm install
npm run dev      # http://localhost:5173
npm run build    # production build to dist/
```
